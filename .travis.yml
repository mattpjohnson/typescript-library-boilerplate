language: node_js

node_js:
  - node
  - '8'
  - '11'

cache:
  yarn: true
  directories:
    - node_modules

install:
  - yarn install

script:
  - yarn test:coverage

after_success:
  - npx codecov

branches:
  only:
    - master
